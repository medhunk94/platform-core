# Checkout Service - Development Environment Overrides

# Reduce replicas for local development
replicaCount: 1

# Lower resource requirements for kind cluster
resources:
  requests:
    memory: "64Mi"
    cpu: "50m"
  limits:
    memory: "128Mi"
    cpu: "100m"

# Development-specific configuration
config:
  PORT: "8080"
  KAFKA_BROKER: "kafka.kafka.svc.cluster.local:9092"
  KAFKA_TOPIC: "dev-order-topic"
